interface:
  display_name: "Agent Team"
  short_description: "Scoped expert orchestration with evidence-based acceptance"
  default_prompt: "Use $agent-team to coordinate only the needed experts, enforce scoped authority, and verify completion with evidence in my language."
