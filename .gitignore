.DS_Store
*.swp
.idea/
.vscode/

# Local keys / env (never commit)
*.key
.env
.seal-key

# Author-only packing helpers (optional; not required for public install)
scripts/seal.sh
scripts/unseal.sh
sealed/
